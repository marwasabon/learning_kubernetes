apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: myapp
spec:
  selector:
    app: springboot
  ports:
    - protocol: TCP
      port: 8030
      targetPort: 8030
  type: ClusterIP

