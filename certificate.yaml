apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: portf-tech-cert
  namespace: default
spec:
  secretName: portf-tech-tls   # <-- cert-manager creates this
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - portf.tech

